-- Historical placeholder. Some local checkouts generated this folder with a
-- Prisma draft that never landed in git. Later migrations apply the real
-- catalogue schema (`unitLabelId`, pricing defaults, drafts). This file must
-- remain a no-op so `prisma migrate reset` can replay the chain.
SELECT 1;
